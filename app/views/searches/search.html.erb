<h2><%= @range %> search for "<%= params[:word] %>"</h2>
<div class="container">
  <div class="row">
    <% if @range == "User" %>
      <%= render 'users/index', users: @users %>
    <% else %>
      <%= render 'books/index', books: @books %>
    <% end %>
  </div>
</div>