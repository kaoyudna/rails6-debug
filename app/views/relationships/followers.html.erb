<h1>Follower Users</h1>
<div class="container">
  <div class="row">
    <% if @users.exists? %>
      <%= render 'relationships/index', users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>
